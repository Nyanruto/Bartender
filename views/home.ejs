<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to the Cocktail Finder</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <div class="container">
    <div id="main-content">
      <h1>Cocktail Discovery</h1>
      <p>Unearth delightful recipes with our random generator, or search for a classic by its name.</p>
      <a href="/generator" class="btn action-btn">Get a Random Cocktail</a>
      <hr style="margin: 40px auto; width: 50%; border: 1px solid var(--card-border);">
      <h2>Search for a Cocktail</h2>
      <form action="/search" method="POST" style="display: flex; justify-content: center; gap: 10px;">
        <input type="text" name="cocktailName" placeholder="e.g., Margarita" required style="padding: 14px; border-radius: 8px; border: 1px solid #ccc; font-size: 16px; background: #333; color: white;">
        <button type="submit" class="btn action-btn" style="margin-top: 0;">Search</button>
      </form>
    </div>

    <div id="loading-spinner" class="spinner hidden"></div>
  </div>

  <script>
    const actionButtons = document.querySelectorAll('.action-btn');
    const mainContent = document.getElementById('main-content');
    const spinner = document.getElementById('loading-spinner');

    actionButtons.forEach(button => {
      button.addEventListener('click', () => {
        mainContent.classList.add('hidden');
        spinner.classList.remove('hidden');
      });
    });
  </script>
</body>
</html>